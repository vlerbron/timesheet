import 'package:flutter/material.dart';

class AccountExpense extends StatelessWidget {
  const AccountExpense({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Expense');
  }
}
